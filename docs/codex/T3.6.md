## T3.6

What changed: rewired the competitors page to fetch by `?id=`, render `null` values as em dashes, highlight the subject row, and explain each numeric column with derivation tooltips.

Cypher used: none directly in this task; data comes from `GET /analyze/{aid}/competitors`.

Couldn’t do: the tooltip copy documents placeholder fields where the backend intentionally returns `null`.
