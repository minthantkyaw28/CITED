## T1.1

What changed: added `GET /analyze/{aid}/dashboard` with a typed `DashboardPayload`, real graph-derived KPI cards, citations-by-model aggregation, competitor bars, quick insights, and top-level Cypher provenance.

Cypher used: `ANALYSIS_TOTALS_CYPHER`, `BRAND_METRICS_CYPHER`, `CITATIONS_BY_MODEL_CYPHER`, `COMPETITOR_DIFF_CYPHER`, plus shared `TOPIC_GAP_CYPHER` and `OUTREACH_CYPHER`.

Couldn’t do: `visibilityTrend` remains empty by design because the graph only stores a single snapshot per analysis.
<!-- chore: note 2026-06-16T22:01:42 -->
