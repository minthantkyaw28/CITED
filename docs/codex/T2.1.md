## T2.1

What changed: added a typed frontend API layer for analysis start, status, graph, dashboard, competitors, recommendations, and SSE subscription.

Cypher used: none directly in this task; the client consumes backend read-model endpoints that carry Cypher provenance.

Couldn’t do: recommendations cards are normalized to `[]` until the backend adds the planned `cards` field.
