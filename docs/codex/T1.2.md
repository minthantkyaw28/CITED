## T1.2

What changed: added `GET /analyze/{aid}/competitors` with sorted brand rows, subject highlighting, nullable placeholder fields for not-yet-derived columns, and an intent-derived insight summary.

Cypher used: shared `ANALYSIS_TOTALS_CYPHER` and `BRAND_METRICS_CYPHER`, plus `INTENT_LEADERSHIP_CYPHER`.

Couldn’t do: `semanticClarity` and `aiReadability` remain `null` because the current graph schema does not expose a credible derivation yet.
